% !TEX root = ../Thesis.tex

\chapter{基于公开数据集的神经网络重建实验}\label{chap:preliminary}

{
本章围绕公开的“散斑—目标图像”配对数据集开展前期研究工作：基于文献\citep{sun2024ol_calibrationfree}公开的数据集，对神经网络重建方法进行验证实验，并通过结构相似度（SSIM）衡量手写数字数据集与服装数据集的重建效果。本章的目的在于建立公开数据集上的验证流程与对照基准，为第\ref{chap:method}章的自建实验系统与方法验证提供参照。

\section{公开数据集与任务定义}\label{sec:public_dataset}

\subsection{数据集来源}\label{sec:dataset_source}

本研究第三章主要参考文献\citep{sun2024ol_calibrationfree}末尾公开的数据集，该工作面向多芯光纤（Multi-core Fiber, MCF）定量相位成像任务，构建了端到端深度学习的“散斑到相位”重建范式，并公开了两份实验数据集：手写数字数据集与服装数据集及其对应的光学散斑图像\citep{sun2023_figshare_dataset1,sun2023_figshare_dataset2}。

从本论文的角度看，这两份公开数据集为“散斑图像重建”提供了良好的验证平台：输入为检测端采集的散斑强度图，输出为对应的目标图像（ground truth，本文用“目标图像”统称其相位/强度表征），可将问题统一为监督学习的图像到图像映射任务。

\subsection{任务定义与符号约定}\label{sec:task_definition}

设散斑图像为$S\in\mathbb{R}^{H\times W}$，目标图像为$Y\in\mathbb{R}^{H\times W}$，希望学习一个可微映射$f_{\theta}(\cdot)$使得：
\begin{equation}
\hat{Y} = f_{\theta}(S),
\end{equation}
其中$\theta$为网络参数，$\hat{Y}$为重建结果。训练阶段通过最小化损失函数$\mathcal{L}(\hat{Y}, Y)$对$\theta$进行优化；测试阶段对未见样本进行推理并评估重建质量与推理速度。

\subsection{数据集特征与划分}\label{sec:dataset_stats}

文献\citep{sun2024ol_calibrationfree}报告其公开数据集包含大量成对样本（散斑、目标图像），并包含两类目标内容：手写数字与服装图像。为了便于复现，本章采用与其一致或近似的训练/验证/测试划分策略：
\begin{itemize}
    \item \textbf{训练集}：用于网络参数学习。
    \item \textbf{验证集}：用于选择超参数与观察收敛情况。
    \item \textbf{测试集}：用于最终性能评估与可视化展示。
\end{itemize}

由于数据集在不同版本或下载方式下可能存在轻微差异，本论文在实现时以“实际下载后的样本数”为准，并在实验记录中固定版本号与划分随机种子（详见第\ref{chap:method}章的工程化实现与复现实验设置）。

\subsection{数据预处理}\label{sec:dataset_preprocess}

为提升训练稳定性与收敛速度，本章对输入散斑与目标图像采用一致的数据预处理流程：
\begin{enumerate}
    \item \textbf{尺寸统一}：将输入与输出统一到网络所需的固定尺寸（$H\times W$），必要时使用中心裁剪/缩放/零填充。
    \item \textbf{强度归一化}：将散斑图像与目标图像归一化到$[0,1]$或$[-1,1]$区间，并记录归一化策略以保证可复现。
    \item \textbf{数据增强（可选）}：对训练集进行随机翻转、旋转、噪声扰动等增强，以提升泛化能力（是否启用与增强参数在实验记录中明确给出）。
\end{enumerate}

\section{神经网络模型与实验设置}\label{sec:baseline_unet}

\subsection{网络模型说明}\label{sec:why_unet}

本章采用编码器-解码器结构的神经网络作为重建模型，用于学习散斑图像到目标图像的映射关系。该类模型具备多尺度特征提取与细节恢复能力，适用于图像到图像的重建任务。更进一步的网络结构改进将在第\ref{chap:method}章中结合自建实验数据展开。

\subsection{实现与训练细节}\label{sec:unet_detail}

为便于复现与后续章节对比，本章固定输入输出尺寸、训练轮数、批大小、学习率调度等关键设置，并记录数据划分与随机种子。若你希望把具体数值写成表格，可以在此处补充一张“实验设置表”。

\noindent\draftmark{待你填写：训练轮数、批大小、学习率、输入/输出尺寸等关键参数。}

% 注：图\ref{fig:unet_arch}为参考文献原图的复用，已在中英文图题中注明来源。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{Img/3/3-1.png}
    \captionsetup{justification=centering,singlelinecheck=false}
    \bicaption{\enspace U-Net网络结构示意图\citep{sun2024ol_calibrationfree}}
              {\enspace U-Net Network Architecture Diagram\citep{sun2024ol_calibrationfree}}
    \label{fig:unet_arch}
\end{figure}

\section{训练策略与评价指标}\label{sec:train_metric}

\subsection{损失函数}\label{sec:loss}

本章以像素级回归为主，采用MAE（L1）损失作为基础损失：
\begin{equation}
\mathcal{L}_{\text{MAE}} = \frac{1}{N}\sum_{i=1}^{N}|\hat{Y}_i - Y_i|.
\end{equation}
在后续实验中也可按需要加入SSIM损失或频域约束等复合损失（第\ref{chap:method}章将结合自建数据进一步讨论）。

\subsection{优化器与学习率策略}\label{sec:optimizer}

优化器采用Adam，并参考文献\citep{sun2024ol_calibrationfree}的设置：初始学习率为$1\times 10^{-4}$，并按固定步长衰减；批大小设置为64。为保证可复现，本章固定训练策略与训练轮数。

\subsection{评价指标：SSIM}\label{sec:metrics}

本章使用结构相似度（Structural Similarity Index Measure, SSIM）作为主要评价指标。SSIM用于衡量两幅图像在亮度、对比度与结构信息上的一致性，其取值通常在$[0,1]$范围内，值越大表示重建图像与目标图像越相似。为清晰起见，本文报告两类数据集测试集上的平均SSIM作为定量结果。

\noindent\draftmark{待你填写：手写数字数据集与服装数据集的平均SSIM。}

\section{实验结果与分析}\label{sec:results_public}

\subsection{定性结果展示}\label{sec:qualitative}

本节分别给出“手写数字数据集”和“服装数据集”的重建结果示例，建议每组样本展示：目标图像、输入散斑、重建结果。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{Img/3/3-2.png}
    \bicaption{\enspace 手写数字数据集的重建结果}{\enspace Reconstruction examples on handwritten digits dataset}
    \label{fig:public_digits}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{Img/3/3-3.png}
    \bicaption{\enspace 服装数据集的重建结果示例}{\enspace Reconstruction examples on fashion dataset}
    \label{fig:public_fashion}
\end{figure}

\subsection{定量对比}\label{sec:quantitative}

本节汇总两类数据集测试集上的平均SSIM，用于量化重建效果并进行对照分析。

\begin{table}[!htbp]
    \centering
    \bicaption{\enspace 公开数据集上的SSIM评价结果}{\enspace SSIM results on public datasets}
    \label{tab:public_quant}
    \footnotesize
    \setlength{\tabcolsep}{6pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{lcc}
        \hline
        数据集 & 指标 & 数值（↑）\\
        \hline
        手写数字数据集 & SSIM & \draftmark{待填} \\
        服装数据集 & SSIM & \draftmark{待填} \\
        \hline
    \end{tabular}
\end{table}

\subsection{与参考文献结果的关系}\label{sec:compare_paper}

文献\citep{sun2024ol_calibrationfree}报告其端到端网络在实验数据上可实现毫秒级重建，并取得较高保真度。由于网络实现细节、数据集划分、预处理方式等会影响结果，因此本章更强调“公开数据集验证流程”与“基于SSIM的对照结论”。在后续章节中，我们将把这些结论迁移到自建实验系统，并进一步评估对光纤扰动、噪声以及跨域泛化的影响。

% 注：本章不展开网络结构改进细节，相关内容将在第\ref{chap:method}章结合自建实验系统进行详细阐述。

\section{结论性观察与对后续工作的启示}\label{sec:insights_to_next}

通过公开数据集的重建实验与SSIM定量评估，本章形成以下结论性观察（建议你结合实验结果补充具体数据支撑）：
\begin{enumerate}
    \item \textbf{端到端网络能够有效学习散斑到目标图像的映射}：在手写数字与服装两类数据上均能实现可辨识的重建结果。
    \item \textbf{数据复杂度提升会显著增加重建难度}：服装数据集的纹理与结构更复杂，模型更容易出现细节缺失或局部伪影。
    \item \textbf{训练与预处理细节对结果影响显著}：归一化方式、数据增强、学习率策略等都会影响收敛与泛化。
\end{enumerate}

基于上述观察，第\ref{chap:method}章将围绕“自建实验系统的数据集构建”和“面向真实实验场景的鲁棒重建”展开，并进一步给出网络结构改进与实验验证。

\section{本章小结}

本章基于文献\citep{sun2024ol_calibrationfree}公开的数据集建立了散斑图像重建的验证流程，并使用SSIM对手写数字与服装两类数据集的重建效果进行量化评估。公开数据集上的实验为后续章节基于自建实验系统的数据集构建、方法设计与性能评估提供了对照基准。

}

